import { DirektivaDirective } from './direktiva.directive';

describe('DirektivaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirektivaDirective();
    expect(directive).toBeTruthy();
  });
});
