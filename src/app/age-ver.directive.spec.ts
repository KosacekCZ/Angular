import { AgeVerDirective } from './age-ver.directive';

describe('AgeVerDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeVerDirective();
    expect(directive).toBeTruthy();
  });
});
